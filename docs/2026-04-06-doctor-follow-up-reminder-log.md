# 2026-04-06 医生端随访提醒增强记录

## 本轮完成

- 医生问诊列表新增“待随访”和“已逾期随访”统计
- 医生问诊列表新增“随访筛选”
  - 全部随访
  - 待随访
  - 今日到期
  - 已逾期
- 医生问诊列表新增“随访提醒”列
- 问诊详情页新增随访提醒提示
- 医生工作台“待随访”入口可直接带筛选跳转到问诊列表
- 医生列表打开详情和关闭详情时会保留当前筛选条件

## 后端改动

- 扩展 `DoctorWorkspaceServiceImpl.consultationList(...)`
- 问诊列表现在会一并返回：
  - 最新医生处理记录
  - 最新医生结论
  - 最近一次随访记录

这样前端在不额外逐条请求详情的情况下，也能直接判断：

- 是否仍需随访
- 随访预计到期时间
- 当前是否已经逾期

## 前端改动

- `DoctorConsultationPage.vue`
  - 增加随访状态统计
  - 增加随访筛选下拉
  - 增加随访提醒列
  - 增加详情页随访告警
  - 保留列表筛选上下文
- `DoctorWorkbenchPage.vue`
  - “待随访”卡片底部按钮改为直接进入待随访列表

## 价值

- 医生能更快定位已经完成接诊、但仍需继续跟进的问诊
- 工作台和问诊列表之间的待办链路更顺
- 为后续继续做“随访到期提醒”“逾期优先排序”“随访闭环统计”打下基础

## 验证结果

- 前端执行 `npm run build` 通过
- 后端执行 `mvn -q -DskipTests compile` 通过

## 后续建议

- 增加“按到期时间排序”的医生列表快捷开关
- 增加“只看我负责且已逾期”的快捷入口
- 增加随访闭环率和逾期率统计看板
